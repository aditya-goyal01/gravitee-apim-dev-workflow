name: Code Scanning

on:
    push:
        branches: [main]
    pull_request:
        branches: [main]
    schedule:
        - cron: "30 5 * * 1"

permissions:
    contents: read

jobs:
    shellcheck:
        name: ShellCheck
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # actions/checkout v4

            - name: Run ShellCheck
              run: |
                  find . -name '*.sh' -not -path './.git/*' \
                    | xargs shellcheck --severity=warning -e SC1091

    test-hooks:
        name: Hook Tests
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # actions/checkout v4

            - name: Install jq
              run: sudo apt-get install -y jq

            - name: Run test suite
              run: bash test-hooks.sh
